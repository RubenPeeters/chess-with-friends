# Vite loads this automatically in `npm run dev`.
# WebSocket connects directly to Caddy — do not route through Vite's dev proxy,
# which conflicts with Vite's own HMR WebSocket on the same port.
VITE_WS_BASE_URL=ws://localhost
